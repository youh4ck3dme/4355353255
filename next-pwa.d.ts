// This declaration file tells TypeScript how to handle the 'next-pwa' module,
// which does not come with its own type definitions. By declaring it, we
// prevent TypeScript from throwing a "Could not find a declaration file" error (TS7016).

declare module 'next-pwa';
