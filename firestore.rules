rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /projects/{projectId} {
      allow read: if true;   // čítať môže každý
      allow write: if false; // zapisovať nikto z klienta
    }
  }
}
